let arr = [1, 2, 15];

let outArray = arr.sort();

console.log(outArray); //[1, 15, 2]

//It sorts the array in the right order after converting it to strings thats why the order of the array changed from its original order