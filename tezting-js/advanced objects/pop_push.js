//Note: These work with the ends of an array, pop works with LIFO, push works with LIFO.


let fruits = ["appple", "pear", "plum", "melon"];

console.log(fruits.pop()); //Deletes the last item of the array

console.log(fruits);

fruits.push("melon"); //puts back the item that it takes in as an argument

console.log(fruits);