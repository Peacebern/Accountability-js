arr = ["jason", "I", "does", "sability", "aryaStarr"];

arr.splice(0, 2, "oluchi", "mentor"); //Note: splice function doesn't only delete array items but we can also use it to insert array items

console.log(arr);