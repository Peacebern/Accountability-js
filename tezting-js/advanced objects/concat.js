card = ["conatct", "wedding", "company", "atm", "balck"];

//Note: syntax array.concat(arg1, arg2), this function copies the array and its argument into a new array.
// console.log(card.concat("visa", "master"));

concard = card.concat("visa", "master");

console.log(concard);