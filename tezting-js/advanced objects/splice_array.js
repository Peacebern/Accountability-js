arr = ["jason", "I", "does", "sability", "aryaStarr"];

arr.splice(3, 1); //Note: 3 means start from index 3, while 2 means delete 2 items.so syntex is array.splice(start, howmanyTodelete);

console.log(arr);